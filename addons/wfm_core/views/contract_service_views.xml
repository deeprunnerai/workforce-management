<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Contract Service Form View -->
    <record id="view_wfm_contract_service_form" model="ir.ui.view">
        <field name="name">wfm.contract.service.form</field>
        <field name="model">wfm.contract.service</field>
        <field name="arch" type="xml">
            <form string="Contract Service">
                <header>
                    <button name="action_activate" string="Activate" type="object"
                            invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_complete" string="Complete" type="object"
                            invisible="state != 'active'" class="btn-primary"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state in ('completed', 'cancelled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,completed"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(wfm_core.action_wfm_installation_services)d" type="action"
                                class="oe_stat_button" icon="fa-building"
                                context="{'search_default_contract_service_id': id}">
                            <field name="installation_service_count" widget="statinfo" string="Installations"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="code"/>
                        <h1>
                            <field name="code" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Service Details">
                            <field name="contract_id"/>
                            <field name="client_id"/>
                            <field name="service_type"/>
                        </group>
                        <group string="Period">
                            <field name="start_date"/>
                            <field name="end_date"/>
                        </group>
                    </group>
                    <group>
                        <group string="Hourly Services">
                            <field name="assigned_hours"/>
                            <field name="price_per_hour"/>
                        </group>
                        <group string="Fixed Services">
                            <field name="quantity"/>
                            <field name="price_per_unit"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="revenue" class="oe_subtotal_footer_separator"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Installation Services" name="installation_services">
                            <field name="installation_service_ids">
                                <list>
                                    <field name="code"/>
                                    <field name="installation_id"/>
                                    <field name="partner_id"/>
                                    <field name="assigned_hours"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Contract Service Tree View -->
    <record id="view_wfm_contract_service_tree" model="ir.ui.view">
        <field name="name">wfm.contract.service.tree</field>
        <field name="model">wfm.contract.service</field>
        <field name="arch" type="xml">
            <list string="Contract Services" decoration-muted="state == 'cancelled'">
                <field name="code"/>
                <field name="contract_id"/>
                <field name="client_id"/>
                <field name="service_type"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="assigned_hours"/>
                <field name="price_per_hour"/>
                <field name="revenue"/>
                <field name="installation_service_count"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'active'"
                       decoration-info="state == 'draft'"
                       decoration-muted="state == 'completed'"/>
            </list>
        </field>
    </record>

    <!-- Contract Service Search View -->
    <record id="view_wfm_contract_service_search" model="ir.ui.view">
        <field name="name">wfm.contract.service.search</field>
        <field name="model">wfm.contract.service</field>
        <field name="arch" type="xml">
            <search string="Search Contract Services">
                <field name="code"/>
                <field name="contract_id"/>
                <field name="client_id"/>
                <field name="service_type"/>
                <filter string="Active" name="active_services" domain="[('state', '=', 'active')]"/>
                <filter string="Physician" name="physician" domain="[('service_type', '=', 'physician')]"/>
                <filter string="Safety Engineer" name="safety_engineer" domain="[('service_type', '=', 'safety_engineer')]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter string="Contract" name="group_contract" context="{'group_by': 'contract_id'}"/>
                    <filter string="Client" name="group_client" context="{'group_by': 'client_id'}"/>
                    <filter string="Service Type" name="group_service_type" context="{'group_by': 'service_type'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Contract Service Action -->
    <record id="action_wfm_contract_services" model="ir.actions.act_window">
        <field name="name">Contract Services</field>
        <field name="res_model">wfm.contract.service</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active_services': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Contract Service
            </p>
            <p>
                Contract Services define specific OHS services (Physician, Safety Engineer)
                with hours and pricing for each contract period.
            </p>
        </field>
    </record>
</odoo>
