<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- WhatsApp Message List View -->
    <record id="wfm_whatsapp_message_list" model="ir.ui.view">
        <field name="name">wfm.whatsapp.message.list</field>
        <field name="model">wfm.whatsapp.message</field>
        <field name="arch" type="xml">
            <list string="WhatsApp Messages"
                  decoration-success="status == 'sent' or status == 'delivered'"
                  decoration-danger="status == 'failed'"
                  decoration-muted="status == 'pending'">
                <field name="sent_at"/>
                <field name="partner_id"/>
                <field name="phone"/>
                <field name="message_type"/>
                <field name="visit_id" optional="show"/>
                <field name="status" widget="badge"
                       decoration-success="status in ('sent', 'delivered', 'read')"
                       decoration-danger="status == 'failed'"
                       decoration-warning="status == 'pending'"/>
                <field name="twilio_sid" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- WhatsApp Message Form View -->
    <record id="wfm_whatsapp_message_form" model="ir.ui.view">
        <field name="name">wfm.whatsapp.message.form</field>
        <field name="model">wfm.whatsapp.message</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Message">
                <header>
                    <button name="action_send" string="Send Now" type="object"
                            class="btn-primary" invisible="status != 'pending'"/>
                    <field name="status" widget="statusbar"
                           statusbar_visible="pending,sent,delivered"/>
                </header>
                <sheet>
                    <group>
                        <group string="Recipient">
                            <field name="partner_id"/>
                            <field name="phone"/>
                        </group>
                        <group string="Details">
                            <field name="message_type"/>
                            <field name="visit_id"/>
                            <field name="sent_at"/>
                        </group>
                    </group>

                    <group string="Message">
                        <field name="message_body" nolabel="1" colspan="2"/>
                    </group>

                    <group string="Twilio Response" invisible="not twilio_sid">
                        <field name="twilio_sid"/>
                        <field name="error_message" invisible="not error_message"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- WhatsApp Message Search View -->
    <record id="wfm_whatsapp_message_search" model="ir.ui.view">
        <field name="name">wfm.whatsapp.message.search</field>
        <field name="model">wfm.whatsapp.message</field>
        <field name="arch" type="xml">
            <search string="Search Messages">
                <field name="partner_id"/>
                <field name="phone"/>
            </search>
        </field>
    </record>

    <!-- WhatsApp Message Action -->
    <record id="action_whatsapp_messages" model="ir.actions.act_window">
        <field name="name">WhatsApp Messages</field>
        <field name="res_model">wfm.whatsapp.message</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No WhatsApp messages yet
            </p>
            <p>Messages sent to partners via WhatsApp will appear here.</p>
        </field>
    </record>

    <!-- Menu Item under Config -->
    <menuitem id="menu_whatsapp_messages"
              name="WhatsApp Log"
              parent="wfm_core.menu_wfm_config"
              action="action_whatsapp_messages"
              sequence="50"/>
</odoo>
